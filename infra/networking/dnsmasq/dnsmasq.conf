#log all dns queries
log-queries
#dont use hosts nameservers
no-resolv
#use google as default nameservers
server=8.8.4.4
server=8.8.8.8
#explicitly define host-ip mappings
address=/test/127.0.0.1

#For Testing oidc flows with apple we need to use a "valid" TLD
# we hijack .grocery here which is reserved for walmart and most probably not used

address=/grocery/127.0.0.1